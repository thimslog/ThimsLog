enum Status {
    ACTIVE
    INACTIVE
    SUSPENDED
}

enum AdminRole {
  SUPER_ADMIN
  ADMIN
  SUPPORT
}

enum InventoryCategoryStatus {
  ACTIVE
  INACTIVE
}

enum InventoryAccountStatus {
  AVAILABLE
  ASSIGNED
  SOLD
  SUSPENDED
  DISABLED
}

enum TransactionType {
  FUNDING   // adding money to wallet
  PAYMENT   // spending wallet balance on a product
  REFUND
  TRANSFER_SENT     // sent funds to another user
  TRANSFER_RECEIVED // received funds from another user
}

enum NotificationType {
  SYSTEM
  TRANSFER_SENT
  TRANSFER_RECEIVED
  ORDER_UPDATE
  WALLET_FUNDED
}

enum TransactionStatus {
  PENDING
  SUCCESS
  FAILED
  UNDERPAID
  OVERPAID
  EXPIRED
}

enum TicketStatus {
  OPEN
  RESOLVED
  CLOSED
}

enum TicketPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}