enum Status {
    ACTIVE
    INACTIVE
    SUSPENDED
}

enum AdminRole {
  SUPER_ADMIN
  ADMIN
  SUPPORT
}

enum InventoryCategoryStatus {
  ACTIVE
  INACTIVE
}

enum InventoryAccountStatus {
  AVAILABLE
  ASSIGNED
  SOLD
  SUSPENDED
  DISABLED
}

enum TransactionType {
  FUNDING   // adding money to wallet
  PAYMENT   // spending wallet balance on a product
  REFUND
}

enum TransactionStatus {
  PENDING
  SUCCESS
  FAILED
  UNDERPAID
  OVERPAID
  EXPIRED
}